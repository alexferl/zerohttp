# Hello World Example

This is the simplest possible zerohttp server.

## Features

- Minimal server setup
- JSON response
- Default configuration

## Running the Example

```bash
go run .
```

The server starts on `http://localhost:8080`.

## Test Commands

```bash
curl http://localhost:8080
```
