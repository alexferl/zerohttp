# WebSocket Example

WebSocket server using gorilla/websocket with zerohttp.

## Running

```bash
go mod tidy
go run .
```

Open http://localhost:8080

## Endpoints

- `GET /` - Web UI
- `GET /ws` - WebSocket endpoint
